# Tasks

## 1. Основа: интервалы, ошибки, модели

- [x] 1.1 Создать пакет `src/mcp_moex/watch/` и `intervals.py`: разбор `15m`/`1h`/`1d` в секунды, границы периода опроса (5 мин...1 сутки) и сводки (от периода опроса...7 суток); проверить `tests/test_watch_intervals.py` (допустимые значения, `1m`, `18:00`, `каждый час`, сводка чаще опроса; тексты ошибок называют параметр и допустимые значения)
- [x] 1.2 Добавить в `errors.py` ошибки «опрос не задан» и «хранилище недоступно» (русский текст для LLM), неверные интервалы и число тикеров оформлять через существующий `InvalidArguments`; проверить тестом, что тексты не содержат трассировок и подсказывают следующий шаг
- [x] 1.3 Добавить в `models.py` входные параметры (`chat_id`, `secids` 1...10, интервалы, `report_ids`) с `Field(description=...)` и выходные модели контракта из `design.md` (`WatchSetResult`, `WatchStopResult`, `WatchStatusResult`, `Report`/`TickerAggregate`, `RunDueResult`, `AckResult`); проверить тестом схем, что у каждого параметра есть описание, а имена и поля совпадают с таблицей контракта

## 2. Хранилище SQLite

- [x] 2.1 Реализовать `watch/store.py`: создание файла и каталогов, `journal_mode=WAL`, `busy_timeout`, `foreign_keys`, схема из `design.md`, версия в `user_version` (новая версия схемы выше известной даёт понятную ошибку); проверить `tests/test_watch_store.py` на временной БД (создание с нуля, повторное открытие, повреждённый файл → ошибка «хранилище недоступно», путь в несуществующем каталоге)
- [x] 2.2 Реализовать операции хранилища: заменить/удалить опрос (с ростом `version`), заявка наступивших опросов со сдвигом `next_poll_at` в одной транзакции `BEGIN IMMEDIATE`, вставка замеров только при прежней `version`, создание сводки с повторной проверкой `next_report_at <= now`, выбор неподтверждённых сводок (лимит 100 и признак «есть ещё»), подтверждение, очистка старше 30 суток; проверить тестами каждой операции, включая замену опроса между заявкой и записью (замеры выбрасываются)
- [x] 2.3 Проверить согласованность двумя соединениями к одному файлу: два «одновременных» прохода заявки не заявляют один опрос дважды, два прохода формирования сводки создают её один раз (тест в `tests/test_watch_store.py`)

## 3. Агрегаты и сводка

- [x] 3.1 Реализовать `watch/report.py` (чистые функции): агрегаты по тикеру (число замеров и сбоев, первая/последняя цена, изменение в %, мин, макс, единица, время последней котировки), сводка без замеров, единственный замер; проверить `tests/test_watch_report.py` на примере 100/105/95/102 (изменение +2%, мин 95, макс 105), единственном замере, тикере из одних сбоев, отсутствии замеров вообще
- [x] 3.2 Реализовать текст сводки по шаблону из `design.md` (период по МСК, строки по тикерам, «% от номинала» для облигаций, оговорка про задержку 15 минут и «не инвестиционная рекомендация»); проверить тестами точный текст и наличие обеих оговорок в любой сводке, в том числе пустой

## 4. Сервис расписания

- [x] 4.1 Реализовать `watch/service.py`: `set` (нормализация и дедупликация тикеров, проверка и первый замер через `get_current_price`, атомарная замена, `replaced`, сохранение неподтверждённых сводок прежнего опроса), `stop`, `status`; проверить `tests/test_watch_service.py` на фейковом ISS и подменённых часах: сценарии первой постановки, замены, дубликатов, неизвестного тикера без порчи прежнего опроса, лимита тикеров, остановки без опроса
- [x] 4.2 Реализовать `run_due` по шагам из `design.md` (заявка → опрос уникальных тикеров с семафором 5 → запись с проверкой версии → сводки → очистка → выдача неподтверждённых и `next_due_at`); проверить тестами: срок не наступил (нет запросов к ISS), срок опроса, срок сводки, пропущенные периоды дают один замер и одну сводку, сбой одного тикера, недоступность ISS целиком (сроки сдвинулись), один тикер в двух чатах опрашивается один раз, `next_due_at = null` без опросов
- [x] 4.3 Реализовать `get_report` (сводка за текущий период без замеров, без сохранения и без сдвига сроков) и `ack` (идемпотентно, число отмеченных); проверить тестами: `watch_status` до и после `get_report` показывает те же сроки, ошибка для чата без опроса, повторное и неизвестное подтверждение, повторная выдача неподтверждённой сводки, `has_more` при более чем 100 сводках
- [x] 4.4 Проверить сквозной сценарий и перезапуск на временном файле БД: постановка → несколько проходов `run_due` → сводка → ack → «перезапуск» (новый объект сервиса на том же файле) → данные и сроки на месте; проверить изоляцию двух чатов (`status`/`get_report`/`stop` одного не касаются другого)

## 5. Инструменты MCP и режим запуска

- [x] 5.1 Добавить `tools/watch.py` с `DESCRIPTION` для каждого из шести инструментов (служебный, для клиента-планировщика, не для ответов пользователю; формат интервалов и границы; смысл `watch_run_due`/`next_due_at`/`watch_ack`) и тонкими функциями к сервису; проверить `tests/test_watch_tools.py`, что описания содержат эти пункты
- [x] 5.2 Расширить `server.py`: `create_server(iss=None, watch_db=None, clock=None)`, регистрация шести инструментов только при `watch_db`, ни одного без признака `readOnlyHint = true`, `_guard` для новых ошибок, дополнение `INSTRUCTIONS`; флаг `--watch-db <путь>` в `build_parser()`/`main()` для обоих транспортов; проверить в `tests/test_server.py`: без флага ровно три инструмента и файл не создаётся, с флагом девять (три с `readOnlyHint = true`, шесть без него), инструмент расписания вызывается через клиент MCP на фейковом ISS (клиент открывать внутри тела теста)
- [x] 5.3 Проверить, что в пакете `watch/` нет `print` (существующий тест на stdout должен покрывать и новые модули) и что запуск `--watch-db` по stdio не пишет в stdout ничего, кроме протокола (расширить `tests/test_transports.py`)

## 6. Документация и проверка на живой бирже

- [x] 6.1 Обновить `README.md` (режим расписания, флаг, шесть инструментов, пример вызовов, контракт результатов) и `CLAUDE.md` (сейчас там «три инструмента только для чтения»: описать опциональный режим, пакет `watch/`, решения про `readOnlyHint` и версию/заявку); проверить, что формулировки согласованы со спеками
- [x] 6.2 Добавить `scripts/smoke_watch.py`: во временной БД по stdio ставит опрос SBER и IMOEX на 5m/1h, вызывает `watch_run_due`, `watch_get_report`, `watch_status`, `watch_stop`, печатает результаты, завершается ненулевым кодом при сбое; проверить запуском при доступном ISS (код 0)
- [x] 6.3 Прогнать `uv run pytest` целиком и `openspec validate add-price-watch-scheduler`; проверить, что все тесты проходят без сети и валидация не даёт ошибок
