# Tasks

## 1. Каркас проекта

- [x] 1.1 Создать `pyproject.toml` (пакет `mcp_moex` в `src/`, Python 3.12, зависимости `mcp`, `httpx`, `pydantic`, dev: `pytest`, `pytest-asyncio`, скрипт `mcp-moex = mcp_moex.server:main`) и проверить, что `uv sync` проходит без ошибок
- [x] 1.2 Создать структуру `src/mcp_moex/` (`server.py`, `iss.py`, `resolver.py`, `models.py`, `tools/search.py`, `tools/price.py`, `tools/history.py`), `tests/`, `scripts/` и проверить, что `uv run python -c "import mcp_moex"` завершается без ошибок
- [x] 1.3 Добавить `.gitignore` (`.venv`, `__pycache__`, `.pytest_cache`) и краткий `README.md` с командой запуска `uv run --directory <путь> mcp-moex`; проверить, что файлы на месте

## 2. Клиент ISS

- [x] 2.1 Реализовать в `iss.py` общий async-клиент (`httpx.AsyncClient`, таймауты, `iss.meta=off`) и разбор таблиц `columns`/`data` в список словарей; проверить юнит-тестом на записанном ответе ISS
- [x] 2.2 Добавить повторы (1–2 раза с задержкой) на сетевые ошибки, 5xx и 429 и доменную ошибку `IssUnavailable`; проверить юнит-тестом с подменённым транспортом `httpx`, что после исчерпания повторов бросается `IssUnavailable`
- [x] 2.3 Добавить кэш в памяти с TTL (10 с для котировок, долгий для справочников) и проверить тестом, что повторный запрос в пределах TTL не обращается к сети, а после истечения обращается
- [x] 2.4 Сохранить записанные ответы ISS в `tests/fixtures/` (SBER, ОФЗ 26238, TMOS, IMOEX, неизвестный тикер, поиск, свечи, `boards`) и проверить, что тесты используют их без доступа к сети

## 3. Модели и ошибки

- [x] 3.1 Описать в `models.py` Pydantic-модели входов (с `Field(description=...)`, границами и значениями по умолчанию) и выходов трёх инструментов по спецификациям и проверить юнит-тестом валидацию границ (`limit` 1–20, формат дат, перечисления)
- [x] 3.2 Определить доменные ошибки (`SecurityNotFound`, `UnsupportedAssetClass`, `RangeTooLarge`, `IssUnavailable`) с текстами на русском (что случилось и что делать) и проверить тестом, что тексты содержат рекомендацию `search_securities` либо список поддерживаемых классов

## 4. Резолвер

- [x] 4.1 Реализовать `resolver.resolve(secid)` по `/securities/{secid}.json`: основная площадка (`is_primary = 1`, движок `stock`), каноническое написание `secid` и `asset_type` по таблице групп из design; проверить тестами на фикстурах для SBER (`TQBR`), ОФЗ 26238 (`TQOB`), TMOS (`TQBR`, а не пустая `TQTF`), IMOEX (`SNDX`) и на регистре (`sber`)
- [x] 4.2 Обработать пустые таблицы как `SecurityNotFound` и неподдерживаемые группы (например, фьючерсы) как `UnsupportedAssetClass`; проверить тестами на неизвестном тикере и на фикстуре срочного контракта
- [x] 4.3 Закэшировать результат резолвинга на длительный срок и проверить тестом, что второй вызов для того же тикера не делает сетевого запроса

## 5. Инструмент `get_current_price`

- [x] 5.1 Реализовать выбор цены для акций и фондов (`LAST`, иначе `PREVPRICE` с `price_source = "previous_close"`), нормализацию `SUR` в `RUB`, `as_of` (`SYSTIME` и `TIME`, `+03:00`), `delayed = true`, `delay_minutes = 15` и `trading_status`; проверить тестами на фикстурах SBER и TMOS, включая случай без сделок
- [x] 5.2 Реализовать облигации: `price_unit = "percent_of_face"`, `face_value`, `currency`, `price_rub`, `accrued_interest`, `dirty_price`, `yield_percent`; проверить тестом на ОФЗ 26238, что `price_rub = price / 100 * face_value` и `dirty_price = price_rub + accrued_interest`
- [x] 5.3 Реализовать индексы (`CURRENTVALUE`, `price_unit = "points"`, `delayed = false`, `delay_minutes = 0`, без полей облигаций) и проверить тестом на IMOEX
- [x] 5.4 Проверить вживую на ISS, что для индекса с основной площадкой `RTSI` (например, `IMOEXCNY`) работают резолвер и цена; при расхождении добавить обработку либо ограничить класс `index` площадкой `SNDX` и синхронизировать `design.md` и спецификацию поиска

## 6. Инструмент `get_price_history`

- [x] 6.1 Реализовать отображение интервалов (`hour` = 60, `day` = 24, `week` = 7, `month` = 31), запрос свечей и преобразование в модели (`begin` с `+03:00`, `volume = null` для индексов, `turnover`); проверить тестами на фикстурах SBER и IMOEX
- [x] 6.2 Реализовать постраничную выборку (`start += 500`, не более 10 страниц) и ошибку `RangeTooLarge` при превышении; проверить тестом с подменённым транспортом на выборке из нескольких страниц и на превышении лимита
- [x] 6.3 Реализовать `summary` (`candles_count`, `start_price`, `end_price`, `min_price`, `max_price`, `change_abs`, `change_pct`, `first_candle`, `last_candle`) по всем свечам и ограничение `candles` последними 300 с `candles_truncated` и `candles_returned`; проверить тестом на ряде из более чем 300 свечей и тестом формулы `change_pct`
- [x] 6.4 Реализовать проверку периода (`date_from` позже `date_till`, формат дат), значение по умолчанию `date_till` (сегодня по Москве) и штатный пустой результат (`candles = []`, `summary = null`, `message`); проверить тестами на каждый из этих случаев
- [x] 6.5 Проверить единицы: облигации в `percent_of_face` без пересчёта, индексы в `points`; проверить тестами на ОФЗ 26238 и IMOEX
- [x] 6.6 Проверить вживую на бумаге со сплитом, скорректированы ли исторические цены ISS; при подтверждении, что они не скорректированы, добавить предупреждение в описание `get_price_history` и в текст `design.md`

## 7. Инструмент `search_securities`

- [x] 7.1 Реализовать запрос `/securities.json` с `is_trading=1`, `group_by`/`group_by_filter` по классам (для `stock` и `fund` по запросу на группу с объединением) и нормализацию в `asset_type` и `board`; проверить тестами на фикстурах «Сбербанк» (`stock`), «золото» (`fund`) и «ОФЗ 26238» (`bond`)
- [x] 7.2 Реализовать постфильтр (исключить площадки `INAV` и `INPF`; для `index` оставить только `SNDX` и `RTSI`) и проверить тестом, что для запроса `TGLD` нет `TGLDA` и `TGLDB`
- [x] 7.3 Реализовать порядок (точное совпадение `secid` или ISIN без учёта регистра первым), `limit`, `count`, `truncated` и пустой результат без ошибки; проверить тестами на `sber`, ISIN `RU0009029540`, `limit = 5` для «банк» и запросе без совпадений

## 8. Сервер MCP

- [x] 8.1 Зарегистрировать три инструмента в `MCPServer` с описаниями (включая подсказку использовать `search_securities` для получения тикера, задержку данных и единицы) и `readOnlyHint`; проверить тестом через клиентскую сессию SDK, что `list_tools` возвращает три инструмента с описаниями параметров и `readOnlyHint = true`
- [x] 8.2 Реализовать возврат результата как `structuredContent` и текстового JSON и преобразование доменных ошибок в результат с `isError = true` без трассировок; проверить тестом, что успешный вызов даёт оба представления, а вызов с неизвестным тикером даёт ошибку с рекомендацией
- [x] 8.3 Реализовать `main()` с выбором транспорта (`stdio` по умолчанию, Streamable HTTP только на `127.0.0.1`, порт настраивается) и проверить вручную, что HTTP-режим не принимает соединения на внешнем интерфейсе
- [x] 8.4 Настроить логирование только в stderr и убедиться, что нигде нет `print` в stdout; проверить тестом, что stdout сервера по `stdio` содержит только сообщения протокола, а диагностика идёт в stderr

## 9. Смоук-клиент и приёмка

- [x] 9.1 Написать `scripts/smoke_client.py` (`stdio_client`, `ClientSession`, запуск `uv run mcp-moex`): `list_tools`, вызов `search_securities`, `get_current_price` и `get_price_history` на реальном ISS, печать результатов и ненулевой код возврата при любой ошибке; проверить запуском, что при доступном ISS код возврата 0
- [x] 9.2 Проверить сценарий сбоя смоук-клиента (например, недоступный ISS или неверная команда сервера) и убедиться, что клиент завершается с ненулевым кодом и указывает упавший шаг
- [x] 9.3 Прогнать полный набор тестов `uv run pytest` и убедиться, что все проходят без доступа к сети
- [x] 9.4 Приёмка: вызвать инструменты через смоук-клиент и сверить результаты с ISS вручную для SBER, ОФЗ 26238, TMOS и IMOEX (цена, единицы, `as_of`, сводка истории)
- [x] 9.5 Дополнить `README.md` описанием инструментов, форматов результатов и примером подключения из проекта агента (`StdioServerParameters` с командой `uv run --directory <путь> mcp-moex`); проверить, что пример запускается смоук-клиентом
